using UnityEngine;

// The GUI class is used as an interface between Unity GUI objects and the rest of the system
// Normally, the GUI class will ONLY send messages to the static game object
// A instance of this class has already been created in the Unity hierarchy and attached as a component to the CANVAS object
// Unity widgets can send messages to the this GUI instance, which relays messages to the static game object

public class Gui : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
